<?php // Parameters & arguments

/**
 * Parameters & Arguments
 * Los parámetros son los datos esperados en las funciones y los argumentos
 * son los valores que se pasan cuando se invoca la función
 */

// Función simple

function foo($x, $y) // Parámetros
{
    return $x * $y;
}

echo foo(5, 10); // Argumentos 
